# Change Log

## [Unreleased]

## [0.1.11] - 2026-06-20

### Added

- Detect `.nrepl-port` in the current directory when no explicit port is supplied.
- Support `:port-file` / `--port-file` for connecting from another directory.
- Document Git `clojure -Ttools install-latest` installation.

### Changed

- Depends on `com.bhauman/rebel-readline` 0.1.11.
